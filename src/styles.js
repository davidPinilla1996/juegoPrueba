//importamos el StyleSheet 
import { StyleSheet } from "react-native";

//exportamos los estilos del src/index.js
export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff' 
    },
  });